all:
    # install pycocotools locally
	python2 setup.py build_ext --inplace
	rm -rf build

install:
	# install pycocotools to the Python site-packages
	python2 setup.py build_ext install --user
	rm -rf build
